# Packt Publishing Video Downloader - Environment Variables
# Rename this file to '.env' to use it

# Your Packt Publishing account credentials
PACKT_EMAIL=your.email@example.com
PACKT_PASSWORD=your_password

# Playlist IDs (comma-separated for multiple playlists)
# These are numeric IDs from playlist URLs (e.g., https://subscription.packtpub.com/playlists/12345)
PACKT_PLAYLIST_IDS=12345, 67890

# Directory to save courses (default: current directory if not specified)
OUTPUT_DIRECTORY=/path/to/save/courses

# Set to 'true' to display the browser window during execution
WATCH=false